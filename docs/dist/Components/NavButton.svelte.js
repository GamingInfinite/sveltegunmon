import './NavButton.svelte.css.proxy.js';
/* src/Components/NavButton.svelte generated by Svelte v3.48.0 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	listen,
	noop,
	safe_not_equal,
	set_data,
	space,
	src_url_equal,
	text
} from "../../snowpack/pkg/svelte/internal.js";

function create_fragment(ctx) {
	let div;
	let img;
	let img_src_value;
	let t0;
	let br;
	let t1;
	let t2;
	let mounted;
	let dispose;

	return {
		c() {
			div = element("div");
			img = element("img");
			t0 = space();
			br = element("br");
			t1 = space();
			t2 = text(/*nav_title*/ ctx[1]);
			if (!src_url_equal(img.src, img_src_value = /*icon_link*/ ctx[0])) attr(img, "src", img_src_value);
			attr(img, "alt", "bruh");
			attr(img, "class", "navimage svelte-1avi79e");
			attr(div, "class", "navitem svelte-1avi79e");
		},
		m(target, anchor) {
			insert(target, div, anchor);
			append(div, img);
			append(div, t0);
			append(div, br);
			append(div, t1);
			append(div, t2);

			if (!mounted) {
				dispose = listen(div, "click", /*handleclick*/ ctx[2]);
				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (dirty & /*icon_link*/ 1 && !src_url_equal(img.src, img_src_value = /*icon_link*/ ctx[0])) {
				attr(img, "src", img_src_value);
			}

			if (dirty & /*nav_title*/ 2) set_data(t2, /*nav_title*/ ctx[1]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(div);
			mounted = false;
			dispose();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { icon_link, nav_title, modal_id } = $$props;

	function handleclick() {
		var modal = document.getElementById(modal_id);
		modal.style.display = "block";
		modal.style.opacity = 0.1;
		modal.style.opacity = 1;
	}

	$$self.$$set = $$props => {
		if ('icon_link' in $$props) $$invalidate(0, icon_link = $$props.icon_link);
		if ('nav_title' in $$props) $$invalidate(1, nav_title = $$props.nav_title);
		if ('modal_id' in $$props) $$invalidate(3, modal_id = $$props.modal_id);
	};

	return [icon_link, nav_title, handleclick, modal_id];
}

class NavButton extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { icon_link: 0, nav_title: 1, modal_id: 3 });
	}
}

export default NavButton;